import mod from './index';
import test from 'ava';

test('Verificando array gerado', t => {
	var rows 	  = 4
		, columns = 3;
	let values = mod(rows, columns);

	// if (values.length == rows && values[0].length == columns) {
	// 	t.pass();
	// }
})

test('', t => {

})
